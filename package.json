{
  "name": "unholy-octopus",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "clean-build": "npm run clean && npm run build",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "prepublish": "npm run clean-build && npm run lint",
    "test": "mocha dist/test/**/*.js",
    "watch": "tsc --watch"
  },
  "keywords": [],
  "author": "David RC Dayton",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^2.2.47",
    "@types/node": "^9.3.0",
    "mocha": "^5.0.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "typescript": "^2.6.2"
  }
}